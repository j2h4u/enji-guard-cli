---
context: default
phase: ddd-convergence
task: 4
total_tasks: 6
status: in_progress
last_updated: 2026-07-16T22:32:05.916Z
---

# DDD convergence handoff

## Critical constraints

- The orchestrator does not write production code; delegate implementation to Luna agents with `fork_turns: "none"`.
- Do not add tests that police historical architecture, removed names, or backward compatibility. Test current behavior and current dependency direction only.
- Do not deploy this feature branch, access host credentials, or make live mutating Enji calls.
- Do not add compatibility aliases, implementation shims, global service locators, or concrete cross-context fallbacks to make old tests pass.
- Continue until an independent current-state architecture audit returns `DDD_COMPLETE`.

<current_state>

Branch `refactor/audit-bounded-contexts` is at `530a126065eeb65bc557134091e351e102c0c753`, equal to origin. The worktree intentionally contains the uncommitted DDD completion batch: 30 modified tracked files plus 6 untracked files, about +1064/-691 tracked lines.

Three ownership workstreams are integrated in the shared tree:

1. Audit/Application: typed catalog observation, schedules, autofixes, email and audit-start workflows moved into Audit; typed `AuditAutofixJob`; Application delegates target resolution to Portfolio.
2. Portfolio: `GatewayPortfolioTargetService`; canonical `RepositoryStatus(repository, audit)` using Audit-owned freshness projection; no Portfolio recomputation of Audit freshness.
3. Auth/Runtime/Gateway: typed auth/runtime/gateway ports; Auth no longer imports Runtime; Runtime no longer imports Auth implementation; Gateway client no longer imports `auth_session.api`; one `AuthSessionAdapter` is intended to be injected from composition.

The convergence agent then partially updated `Application`: facade auth methods, one `AuthSessionAdapter` wired into gateways/runtime, and minor typing fixes. It paused before Delivery and shared-test migration.

</current_state>

<completed_work>

- Published baseline reconstruction commit `530a126` with clean full verification and runtime smoke before this uncommitted batch.
- Added hard transitive AuthSession/Runtime contracts; current `lint-imports` reports 6 kept, 0 broken.
- Removed vocabulary archaeology scanner and historical legacy-facade test per user direction.
- Runtime receives MCP factory/runner through typed callbacks instead of importing Delivery MCP.
- Audit owns catalog observation persistence; Gateway HTTP no longer has catalog observation side effects.
- Audit owns schedule, autofix, email and start workflow decisions.
- Portfolio owns target/scope expansion and carries Audit's typed status projection.

</completed_work>

<remaining_work>

1. Finish Delivery convergence:
   - remove `auth_session.api.AuthError` imports and direct `.auth` calls;
   - call `Application.import_cookie`, `import_bearer`, `auth_status`, and `auth_refresh`, catching `ApplicationAuthError`;
   - remove `enji_gateway.catalog_snapshot` imports, begin/end observation calls, and `_is_audit_aware_operation` business classification;
   - render typed catalog changes exposed by the same Application invocation;
   - pass `runtime_auth=_application().runtime_auth_port()` and injected MCP runner/factory into supervisor.
2. Delete `runtime_observability/runtime.py` shim and migrate all callers/tests to `runtime_observability.supervisor`.
3. Delete obsolete `enji_gateway/catalog_snapshot.py` and migrate its tests to `AuditCatalogObserver`.
4. Update Gateway/HTTP/client tests to pass a fake `GatewayAuthPort`; do not reintroduce defaults or globals.
5. Fix basedpyright:
   - current total: 91 errors;
   - 82 are outdated tests, mainly missing required `auth_port` and runtime-shim internals;
   - source residuals include `audit/catalog_observation.py` payload narrowing and delivery MCP runner typing.
6. Run focused tests and current architecture checks, then an independent DDD audit. Continue fixing real ownership violations until verdict `DDD_COMPLETE`.
7. Only then run full `just verify`, exact-image isolated runtime smoke, commit, and push. No deployment.

</remaining_work>

<decisions_made>

- Intermediate compatibility is unnecessary; prefer clean final ownership over transitional aliases.
- Current architecture contracts are valuable; historical terminology bans are not.
- Application is orchestration/composition only. Delivery calls Application. Gateway translates wire formats. Contexts depend on typed ports, not implementations.
- Runtime owns readiness/telemetry and consumes an injected runtime-auth port. Auth owns credentials/refresh semantics. Gateway consumes an injected gateway-auth port.
- Catalog observation is Audit-owned state, not Delivery or Gateway state.

</decisions_made>

<blockers>

- The tree is not type-clean yet: 91 basedpyright errors.
- Old tests encode constructor defaults and the removed runtime shim; migrate them to current injected seams rather than restoring compatibility.

</blockers>

## Required reading

1. `AGENTS.md` — product invariants and QA rules.
2. `docs/decisions.md` — current bounded-context ownership.
3. `docs/plans/audit-bounded-contexts-refactor.md` — target architecture and acceptance history.
4. `.planning/HANDOFF.json` — machine-readable task state.
5. `src/enji_guard_cli/application.py` — partial convergence point.
6. `src/enji_guard_cli/auth_session/adapters.py`, `runtime_observability/ports.py`, `enji_gateway/ports.py` — new injection seams.

## Infrastructure state

- No background agents should be assumed alive after restart.
- No containers, volumes, watchers, async jobs, or live Enji tasks were started by this pause operation.
- Feature branch is not deployed.

<context>

The user requested a runtime restart but wants work to continue afterward until the architecture is fully DDD. Resume from the existing dirty worktree; do not reset or discard it. The immediate goal is convergence, not another redesign.

</context>

<next_action>

Start with `$gsd-resume-work`, then delegate the Delivery/test convergence steps above to a Luna High worker. After type/test cleanliness, run a fresh read-only DDD ownership audit.

</next_action>
